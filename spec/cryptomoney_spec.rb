require_relative '../lib/cryptomoney'

describe "the crypto function" do
	it "get crypto_nameS" do
		expect(crypto_nameS).to eq([array])
		end
	end
		
	it "get crypto_priceS"
		expect(crypto_priceS) to.eq([array_1])
		end
		
	it "should return hash of currencies and values" do
		expect(result) to.eq({array => array_1})
		end
	end 
